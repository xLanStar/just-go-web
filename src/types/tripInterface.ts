export interface TripInfo {
  id: string;
  user: string;
  userId: string;
  title: string;
  image: string;
  day: number;
  publishDay: string;
  labels: string[];
  like: number;
  isLike: boolean;
}
